[package]
name = "semver-lint"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A streaming linter for semantic version strings"

[[bin]]
name = "semver-lint"
path = "src/main.rs"

[dependencies]
